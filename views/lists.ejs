<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>TO_DO LIST</title>
    <style>
      html {
  background-color: #E4E9FD;
  background-image: -webkit-linear-gradient(65deg, #A683E3 50%, #E4E9FD 50%);
  min-height: 1000px;
  font-family: 'helvetica neue';
}

h1 {
  color: #fff;
  padding: 10px;
}

.box {
  max-width: 900px;
  margin: 50px auto;
  background-color: white;
  border-radius: 5px;
  box-shadow: 5px 5px 15px -5px rgba(0, 0, 0, 0.3);
}

#heading {
  background-color: #A683E3;
  text-align: center;
}

.item {
  min-height: 70px;
  display: flex;
  align-items: center;
  border-bottom: 1px solid #F1F1F1;
}

.item:last-child {
  border-bottom: 0;
}

input:checked+p {
  text-decoration: line-through;
  text-decoration-color: #A683E3;
}

input[type="checkbox"] {
  margin: 20px;
}

p {
  margin: 0;
  padding: 20px;
  font-size: 20px;
  font-weight: 200;
  color: #00204a;
}

form.item {
  text-align: center;
  margin-left: 20px;
}

button {
  min-height: 50px;
  width: 50px;
  border-radius: 50%;
  border-color: transparent;
  background-color: #A683E3;
  color: #fff;
  font-size: 30px;
  padding-bottom: 6px;
  border-width: 0;
}

input[type="text"] {
  text-align: center;
  height: 60px;
  top: 10px;
  border: none;
  background: transparent;
  font-size: 20px;
  font-weight: 200;
  width: 313px;
}

input[type="text"]:focus {
  outline: none;
  box-shadow: inset 0 -3px 0 0 #A683E3;
}

::placeholder {
  color: grey;
  opacity: 1;
}

footer {
  color: white;
  color: rgba(0, 0, 0, 0.5);
  text-align: center;
}
.entry{
  margin: 5px;
}
.container{
  display: grid;
  width: 300px;
  height: 100px;
  align-items: center;
  grid-template-columns: 100px 100px 100px;
  justify-content: center;
  text-align: center;
  font-size: 20px;
}

    </style>
  </head>
  <header>
    <div class="container">
    <a href="/" style="text-decoration: none; color: blue;">Home</a>
    <a href="/about" style="text-decoration: none; color: blue;">About</a>
  </div>

  </header>
  <body>
    
    <div class="box" id="heading">
      <h1 style="color:blue;"><%= heading %> To-Do List</h1>
    </div>

 <div class="box">
  <form class="item "action="/Lists/<%= heading %>" method="post">
    <input class="entry" type="text" name="newItem" placeholder="New Item" autocomplete="off">
    <label for="timeofstart">Start On</label></br>

    <input class="entry" type="time" name="timeofstart" autocomplete="off">
    <input class="entry" type="date" name="dateofstart" autocomplete="off">
    <label for="timeoffinish">Finish By</label></br>
    <input class="entry" type="time" name="timeoffinish" autocomplete="off">
    <input class="entry" type="date" name="dateoffinish" autocomplete="off"> 
    <button class="entry" type="submit" name="button" value= <%= heading %>> + </button>
  </form>
  
  <% for(let index=0; index < bounds ; index++) { %>
    <form action="/Lists/<%= heading %>/delete" method="post">
    <div class="item">
    <input type="checkbox" name="checkBox" value="<%=newListItem[index]._id%>" onchange="this.form.submit()">
    <p style="font-size: 30px;"><%= newListItem[index].name %>
      <span style="font-size: 60%;"></br>Start On  : <%= newListItem[index].dateS %>  Time: <%= newListItem[index].timeS %></span>
     <span style="font-size: 60%; "></br>Finish By : <%= newListItem[index].dateF %>  Time: <%= newListItem[index].timeF %></span>
    </p>
    
     </div>
    </form>
<%  } %>

 
</div>

<%- include('footer'); -%>